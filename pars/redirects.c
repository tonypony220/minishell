#include "../minishell.h"

/* int		check_for_redir(t_shell *shell)
{
	if (shell->cmd->redir_type == 4 || shell->cmd->redir_type == 1)
		redir_in(shell);
	else if (shell->cmd->redir_type == 2 || shell->cmd->redir_type == 3)
		redir_out(shell);
	else
		return (error_out(shell, "impossible"));
} */
